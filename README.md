# Trunk Playground

A tiny TypeScript project used to learn and test the Trunk developer platform.

The implementation for Trunk Code Quality, Flaky Tests, and Merge Queue is being developed on a feature branch so the setup can be reviewed in a pull request.

## Local development

```bash
npm install
npm test
npm run typecheck
```

